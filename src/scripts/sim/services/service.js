import { Room } from '../room.js';

/**
 * A service represents simulation logic that is applied at the room level.
 */
export class Service {
  /**
   * @param {Room} room 
   */
  simulate(room) {
    // Implement in subclass
  }
}